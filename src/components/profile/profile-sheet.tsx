// This component is deprecated in favor of the full-page /profile route.
// It is kept temporarily to avoid build breaks but is no longer used in navigation.
export function ProfileSheet() {
  return null;
}
